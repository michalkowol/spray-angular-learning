(function () {
    'use strict';

    var should = chai.should();

    describe('A orderFeedWithShortId method2', function () {
        it('should return array sorted by orderFeed with all fields from orderFeed and chartItemId from chartsDetails', function () {
            (1 + 2).should.be.equal(3);
        });
    });
})();